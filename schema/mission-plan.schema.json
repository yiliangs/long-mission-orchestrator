{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://long-mission-orchestrator/long-mission-orchestrator/mission-plan.schema.json",
  "title": "Mission plan (plan.json)",
  "description": "Harness-neutral frozen DAG produced by PLAN/FIGHT/FREEZE and consumed by any executor adapter. This artifact is the contract between brain (planner) and hands (executor); it is pure data and must contain no harness-specific constructs. See agent-constitution.md.",
  "type": "object",
  "required": ["schema_version", "run_id", "goal", "repo", "mode", "constitution_version", "nodes"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "const": "0.1" },
    "run_id": {
      "type": "string",
      "description": "Stable id for this mission run. Names the .mission/<run_id>/ directory. Generated by the planner (no Date.now in executor scripts; stamp here)."
    },
    "goal": { "type": "string", "description": "The one-line human goal this mission serves." },
    "repo": { "type": "string", "description": "Path or name of the target repo. Must have a ## Agent contract." },
    "mode": {
      "enum": ["attended", "unattended-live", "queued"],
      "description": "Mid-flight escalation routing only (constitution §4). Does not change rules or floors."
    },
    "constitution_version": {
      "type": "string",
      "description": "Version of agent-constitution.md governing this run. Required for evolution-loop comparability (§7)."
    },
    "compute_role_required": {
      "enum": ["light", "heavy", "any"],
      "default": "any",
      "description": "Coarsest role this mission needs. Bound to a physical machine at runtime via machine-profile.md. A heavy node on a light machine is deferred-with-note or dispatched cross-machine."
    },
    "eta": {
      "type": "string",
      "description": "ETA computed at FREEZE (ISO 8601 or relative). Recomputed during EXECUTE for late-vs-diverging discrimination (§6.3). Not a deadline."
    },
    "deliverable_zones": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Path globs (copied from repo contract) that carry the V2 categorical floor. Mirrored here so the executor is self-contained."
    },
    "nodes": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/node" }
    }
  },
  "$defs": {
    "node": {
      "type": "object",
      "required": ["id", "title", "deps", "parallelizable", "v_class", "ac_required", "acceptance_criteria"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "description": "Unique within the plan. Referenced by deps." },
        "title": { "type": "string" },
        "instruction": { "type": "string", "description": "What the executor agent is told to do for this node." },
        "deps": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Node ids that must complete before this node is ready. Empty = root-ready."
        },
        "parallelizable": {
          "type": "boolean",
          "description": "If true and ready, may fan out alongside sibling parallelizable nodes. Nodes that mutate shared files concurrently get worktree isolation."
        },
        "v_class": {
          "enum": ["V0", "V1", "V2", "V3"],
          "description": "Verification class (§2). Planner may under-specify upward via rounding; close-time binding (§2.1) downgrades V0/V1 to V2 if no closure record is produced."
        },
        "check": {
          "type": ["string", "null"],
          "default": null,
          "description": "Suggested check command for V0/V1, or 'TBD'/null. NOT binding at plan time — the executor binds the concrete check at close time and records it in closure_record. Prefer a name from the repo contract's verifier registry."
        },
        "ac_required": {
          "type": "boolean",
          "description": "Whether this node must be critiqued (§3). MUST be true for V2, for outward-facing/irreversible nodes, and for the final deliverable node (floors, §3.1). May be true above the floor at planner discretion."
        },
        "acceptance_criteria": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" },
          "description": "Named, checkable conditions for done. Blockers must cite one of these by reference (§3.3)."
        },
        "is_sub_loop": {
          "type": "boolean",
          "default": false,
          "description": "If true, this node is a structured sub-loop (§6.1 tier 2): fresh-context agent per iteration, state on disk."
        },
        "caps": {
          "type": "object",
          "additionalProperties": false,
          "description": "Per-node overrides of constitution §6.2 defaults. Each override SHOULD carry a reason.",
          "properties": {
            "micro_loop_retries": { "type": "integer", "minimum": 0 },
            "sub_loop_iterations": { "type": "integer", "minimum": 1 },
            "subtree_replans": { "type": "integer", "minimum": 0 },
            "reason": { "type": "string" }
          }
        },
        "compute_role_required": {
          "enum": ["light", "heavy", "any"],
          "default": "any",
          "description": "Per-node compute requirement (e.g. a training node needs heavy)."
        },
        "is_final_deliverable": {
          "type": "boolean",
          "default": false,
          "description": "Marks the final assembly node. Forces ac_required=true and a 3-lens critic panel (§6.2)."
        }
      }
    }
  }
}
