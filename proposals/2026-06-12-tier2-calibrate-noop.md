# Tier-2 calibration — 2026-06-12 (NO-OP on caps; 2 hygiene findings)

Proposal id: `2026-06-12-tier2-calibrate-noop`
Tier: 2 (`calibrate`) · Governing version at run: **v0.3.3**
Status: **draft** — generated by `/evolve calibrate`, not yet emailed/granted.

## Verdict

**No cap amendment is licensed by the current corpus.** Every cap in §6.2 → **Leave**.
This is a deliberate no-op, recorded so the next cycle has a comparable baseline. Two
data-hygiene findings (below) are the real output; one of them currently *blocks* any future
version-partitioned calibration.

## Corpus read

`claude-fieldnotes/mission-caps.jsonl`, full file = **3 records**:

| run_id | constitution_version | cap_hits | per-cap used/limit |
|---|---|---|---|
| `block-hygiene-20260608` | `0.1` | `[]` | (old per-mission schema; no per-cap fields) |
| `20260611-jobe-submit-audit` | **(absent)** | `[]` | plan_fight 2/3 · audit 1/2 · cold_swaps 1/1 |
| `web-ui-port-20260611` | **(absent)** | `[]` | plan_fight 1/3 · audit 0/2 · cold_swaps 1/1 |

Filtered to the governing version **v0.3.3**, the corpus is **empty**: the only version-tagged
record is v0.1 (a prior regime — the format doc forbids aggregating across versions), and the
two recent M2 records declare no version at all. The trigger threshold (~10 lines since last
calibration, per the skill) is also not met — 3 lines total, no prior cap-calibration marker.

## Heuristic pass (deterministic, per `schema/cap-log.format.md` §"How Tier-2 reads it")

Per `(constitution_version, cap)` group:

- **Raise candidate** = >20% of nodes hit the limit AND most hits `would_have_converged:true`.
  All three records carry `cap_hits:[]` → **0% hit rate** → no raise candidate for any cap.
- **Lower candidate** = p95(`used`) < `limit`/2. Only the two recent records carry used/limit
  pairs (n=2, so p95 ≈ max):

  | cap | used | limit | p95(used) | limit/2 | candidate? |
  |---|---|---|---|---|---|
  | plan_fight_rounds | {2,1} | 3 | 2 | 1.5 | no (2 ≥ 1.5) |
  | audit_cycles | {1,0} | 2 | 1 | 1.0 | no (1 ≥ 1.0, boundary, n=2) |
  | cold_swaps | {1,1} | 1 | 1 | 0.5 | no (1 ≥ 0.5) |

  micro_loop_retries, sub_loop_iterations, subtree_replans: **no observations** → leave.

**Result:** no raise candidates, no lower candidates. §6.2 table unchanged.

## Proposed §6.2 diff

_(none — all caps Leave)_

| cap | current | proposed | evidence_stats | rationale |
|---|---|---|---|---|
| micro-loop retries | 3 | 3 | 0 obs | no data |
| sub-loop iterations | 5 | 5 | 0 obs | no data |
| subtree replans | 2/3 | 2/3 | 0 obs | no data |
| plan-fight rounds | 3 | 3 | p95(used)=2, n=2 | above lower threshold |
| audit cycles | 2 | 2 | p95(used)=1, n=2 | boundary, n too small |
| cold-reviewer swaps | 1 | 1 | p95(used)=1, n=2 | above lower threshold |

## Hygiene findings (evidence-backed; not §6.2 edits, not PERIMETER)

**H1 — recent cap-log records omit the mandatory `constitution_version` field.**
- Evidence: `20260611-jobe-submit-audit`, `web-ui-port-20260611` — both lack `constitution_version`.
- Why it matters: `cap-log.format.md` lists the field as required and the heuristics partition
  *by* `(constitution_version, cap)`. Records without it cannot be assigned to the v0.3.3 group,
  so they are invisible to calibration. **This is the binding blocker:** until the writer stamps
  the governing version, the v0.3.3 corpus stays empty no matter how many missions run.
- Proposed action: fix the DELIVER-time writer (mission-record / cap-log emitter) to stamp
  `constitution_version` on every line; optionally back-fill the two records above as `0.3.3` if
  that was the governing version at 2026-06-11 (confirm before back-fill — do not guess).

**H2 — `20260611-jobe-submit-audit` spawned 38 agents against an `agent_budget` of 36 (overrun of 2).**
- Evidence: that record — `"agents_spawned":38, "agent_budget":36`.
- Why it matters: §6.4 frames budget exhaustion as a *divergence* — the executor should stop
  opening new nodes and finalize gracefully, not exceed the frozen ceiling. A 2-agent overrun is
  either (a) a logging artifact (budget frozen at PLAN, agents counted post-hoc across replans) or
  (b) a real ceiling-enforcement gap. agents_spawned is not one of the six §6.2 caps, so this is a
  budget observation, not a cap proposal.
- Proposed action: none constitutional yet; flag for the executor adapter to confirm whether the
  ceiling is enforced at spawn time. Re-examine if the pattern recurs.

## Predicted effect

Applying this proposal changes **nothing** in `docs/agent-constitution.md` §6.2 (no version bump
on the caps). Acting on H1 unblocks the first real v0.3.3 calibration once ≥10 properly-tagged
records accumulate. No PERIMETER (§9) clause is touched.

## Audit verdict (2026-06-19, `/mission-log-audit` — Human: "go with rec")

- **Cap no-op:** confirmed. §6.2 unchanged. No re-surface.
- **H1 — RESOLVED.** The DELIVER-time writer was already fixed (deployed `~/.claude/commands/mission.md`
  §274–279 now mandates `constitution_version` on every cap-log line; all records from
  `2026-06-12-stackmix-web-port` onward carry it). Only the back-fill remained. Done — but the
  rec's placeholder value (`0.3.3`) was **wrong** and the "confirm before back-fill — do not guess"
  guard caught it: each unstamped line was stamped with the version **verified from its own
  mission-record**, not 0.3.3. Four lines back-filled (not the two originally named — two more
  06-12 lines were also unstamped): `20260611-jobe-submit-audit` → `0.3.1`,
  `web-ui-port-20260611` → `0.3.1`, `arch-cleanup-20260612` → `0.3.2`,
  `2026-06-12-web-port-roommix-packaging` → `0.3.1`. Net effect: lines are now correctly
  version-partitioned (no longer silently dropped), though each per-version group is still n≤2,
  so no calibration triggers yet.
- **H2 — DEFERRED** by the Human; re-examine on recurrence. Note for the next audit: the
  budget-overrun pattern has **already recurred and the logging is inconsistent** —
  `arch-cleanup-20260612` overran `agent_budget` 24→25 *and* recorded the cap_hit; the original
  `20260611-jobe-submit-audit` overran 36→38 but recorded `cap_hits:[]` (the overrun never landed
  in the calibration corpus). So the real H2 issue is twofold: (a) spawn-time ceiling not enforced,
  and (b) the overrun isn't always logged as a §6 `cap_hit` (node `"mission"`). If a third overrun
  appears, promote to investigation.

### H2 update — 2026-06-26 — PROMOTED

The third overrun has appeared: `2026-06-21-thumbnailbar-web-port` overran `agent_budget`
43/40 and **did** record the cap_hit (with the new `would_have_converged: true` field). Per
the rule above, H2 is **PROMOTED to investigation.**

Updated diagnosis from the third data point:
- **Issue (b) — mostly resolved.** Both recent overruns (`arch-cleanup`, `thumbnailbar`)
  logged the `cap_hit`; the original `jobe-submit-audit` gap is an older-writer artifact.
  The current DELIVER-time writer catches mission-level cap_hits reliably.
- **Issue (a) — still live, now thrice-confirmed.** The executor permits
  `agents_spawned > agent_budget` rather than refusing at the ceiling. §6.4 frames budget
  exhaustion as a *divergence* — "stop opening new nodes, let in-flight nodes close" — but
  the observed behavior is "keep opening past the ceiling, record overshoot post-hoc." A
  60% overrun rate (3/5 M2 runs since 2026-06-11) is no longer noise.

  Suggested next step: a focused investigation into where in the executor's spawn loop the
  `agent_budget` check fires (or doesn't) — `budgetExhausted()` in
  `executors/mission-executor.workflow.js` returns the right answer, but the call site that
  consumes it apparently doesn't honor it as a hard stop for the wave currently in flight.
  A real fix should preserve §6.4's "in-flight nodes complete" rule while preventing new
  spawns from being enqueued once the ceiling is crossed.
