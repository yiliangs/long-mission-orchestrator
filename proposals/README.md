# proposals/

Staging area for `/evolve` amendment batches. The scheduled background run writes a draft
`<proposal-id>.md` here and emails it to the Human. Nothing here is applied to the constitution
until `/evolve apply <proposal-id>` runs on an explicit grant. Drafts are kept as an audit
trail of what was proposed (and what was granted vs. dropped).
