# long-mission-orchestrator

- This repository is the canonical source. Never edit deployed `~/.claude` copies as source.
- Keep one owner per mechanism: `skills/` for lifecycle and delivery, `schema/` for data shape, `executors/` for DAG mechanics and integrated audit, `docs/operating-card.md` for worker conduct, and `scripts/mission_heartbeat.ps1` for recovery.
- Preserve Windows PowerShell 5.1 compatibility in `.ps1` files.
- Never add AI attribution to commits or repository files.
